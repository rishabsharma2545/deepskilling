// 1. Create index.html with the proper HTML5 doctype, <html lang='en'>, 
// <head> with charset UTF-8, viewport meta tag, and a descriptive <title>.

// 2. Inside <body>, create the following semantic sections: <header> (site name + navigation),
// <main> with two child sections: <section id='hero'> and <section id='courses'>, and <footer>.

// 3. Inside <header>, add a <nav> with an unordered list of links: Home, Courses, Profile, Grades.

// 4. Inside the hero section, add an <h1> with 'Welcome to the Student Portal', 
// a short <p> description, and a <button>Explore Courses</button>.

// 5. Inside the courses section, add an <h2>, and three <article> elements 
// each containing an <h3> (course name), <p> (description), and a <span> showing credits.

// 6. Validate your HTML at https://validator.w3.org/ — resolve all errors before proceeding.

// 7. Link styles.css to index.html using a <link> tag inside <head>.

// 8. Apply a CSS reset: set margin: 0, padding: 0, box-sizing: border-box on the universal selector (*).

// 9. Style the <body>: set font-family to a system font stack (e.g., Arial, sans-serif), 
// a background colour, and a default text colour.

// 10. Style the <header>: add padding, a distinct background colour, and use 
// display: flex with justifycontent: space-between to separate the site name and nav links.

// 11. Style the nav links: remove list-style, display them inline, add spacing between items, 
// and change the text-decoration.

// 12. Style the hero section: centre the text with text-align: center, add generous padding, 
// and style the button with padding, a border, and a hover state using the :hover pseudo-class.

// 13. Add a CSS class .course-card to each article and apply padding, a border, border-radius, and a box shadow.